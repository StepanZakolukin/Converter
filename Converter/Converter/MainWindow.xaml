<Window x:Class="Converter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Converter"
        mc:Ignorable="d"
        Title="Конвертер"
        Height="450"
        Width="800"
        WindowState="Maximized">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0" Margin="0, 0, 0, 10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Margin="5" Command="{Binding LoadFileCommand}">Загрузить файл</Button>
            <Button Grid.Column="1" Margin="5" Command="{Binding TransformFileCommand}">Преобразовать</Button>
        </Grid>
        
        <Grid Grid.Row="1" Margin="0, 0, 0, 10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Margin="5">
                <TextBlock Text="Фамилия:"/>
                <TextBox Height="20" Text="{Binding NewLastName, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="5">
                <TextBlock Text="Имя:"/>
                <TextBox Height="20" Text="{Binding NewFirstName, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            <StackPanel Grid.Column="2" Margin="5">
                <TextBlock Text="Сумма:"/>
                <TextBox Height="20" Text="{Binding NewAmount, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            <StackPanel Grid.Column="3" Margin="5">
                <TextBlock Text="Месяц:"/>
                <ComboBox Height="20" ItemsSource="{Binding MonthList}" SelectedItem="{Binding SelectedMonthRu}" />
            </StackPanel>
            <Button Height="20" Grid.Column="4" VerticalAlignment="Bottom" Margin="5" 
                    Command="{Binding AddEntryCommand}">Добавить</Button>
        </Grid>
        
        <DataGrid Grid.Row="2"
                  IsReadOnly="True"
                  ItemsSource="{Binding Employees}"
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Auto"
                  FrozenColumnCount="2">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="100"/>
                <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="100"/>
                <DataGridTextColumn Header="Январь" Binding="{Binding January, StringFormat={}{0:N2}}" Width="*"/>
                <DataGridTextColumn Header="Февраль" Binding="{Binding February, StringFormat={}{0:N2}}" Width="*"/>
                <DataGridTextColumn Header="Март" Binding="{Binding March, StringFormat={}{0:N2}}" Width="*"/>
                <DataGridTextColumn Header="Апрель" Binding="{Binding April, StringFormat={}{0:N2}}" Width="*"/>
                <DataGridTextColumn Header="Май" Binding="{Binding May, StringFormat={}{0:N2}}" Width="*"/>
                <DataGridTextColumn Header="Июнь" Binding="{Binding June, StringFormat={}{0:N2}}" Width="*"/>
                <DataGridTextColumn Header="Июль" Binding="{Binding July, StringFormat={}{0:N2}}" Width="*"/>
                <DataGridTextColumn Header="Август" Binding="{Binding August, StringFormat={}{0:N2}}" Width="*"/>
                <DataGridTextColumn Header="Сентябрь" Binding="{Binding September, StringFormat={}{0:N2}}" Width="*"/>
                <DataGridTextColumn Header="Октябрь" Binding="{Binding October, StringFormat={}{0:N2}}" Width="*"/>
                <DataGridTextColumn Header="Ноябрь" Binding="{Binding November, StringFormat={}{0:N2}}" Width="*"/>
                <DataGridTextColumn Header="Декабрь" Binding="{Binding December, StringFormat={}{0:N2}}" Width="*"/>
                <DataGridTextColumn Header="ИТОГО" Binding="{Binding Total, StringFormat={}{0:N2}}" Width="*" FontWeight="Bold" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
